import 'package:flutter/material.dart';

class PosmScreen extends StatelessWidget {
  const PosmScreen({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("POSM Screen")),
    );
  }
}